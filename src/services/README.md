# Services

API and Firebase data access layer. UI and features call services — never Firebase or fetch directly from components.

```
services/
├── auth.service.ts      # (Auth module)
├── student.service.ts   # (Student module)
└── ...
```
